{
  "name": "wanderparkplatz",
  "private": true,
  "type": "module",
  "workspaces": [
    "pipeline",
    "web"
  ],
  "scripts": {
    "data:fetch": "npm run -w pipeline fetch",
    "data:build": "npm run -w pipeline build",
    "data:load": "npm run db:setup && npm run -w pipeline load",
    "dev": "npm run -w web dev",
    "test": "npm run -w pipeline test",
    "db:setup": "npm run -w pipeline setup",
    "data:bilder": "npm run -w pipeline bilder",
    "backup": "npm run -w pipeline sicherung",
    "data:aktualisieren": "npm run backup && npm run -w pipeline fetch admin pois places plz trails ziele umfeld wasserfaelle && npm run data:build && npm run data:load && npm run data:bilder"
  }
}